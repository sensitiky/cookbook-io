import { ReactNode } from 'react';

export interface UserType {
  userID: number;
  name: string;
  lastName: string;
  Email: string;
}
export interface GradientBackgroundProps {
  children: ReactNode;
}
